import Navigator from './navBar';
export default class Simulator extends React.Component {
    render () {
        return (
            <Navigator>
                
            </Navigator>
        );
    }
}